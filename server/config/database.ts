import dotenv from "dotenv";

dotenv.config();

/**
 * Database configuration file (placeholder for future database integration)
 * Currently not in use as we're using direct API calls to Helius
 */

export const dbConfig = {
  // This is a placeholder for future database connection settings
  // Currently the app uses direct API calls rather than a database
  connected: false,
  type: "none"
};

export default dbConfig;
